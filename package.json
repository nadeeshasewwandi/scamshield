{
  "name": "spam-detector-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview -- --host 127.0.0.1 --port 4173"
  }
}
